# Docker

Docker Engine y Compose se instalan mediante paquetes del sistema durante el
bootstrap inicial. Antes de ejecutar cargas ML se deben medir y documentar
límites de CPU, memoria, swap, almacenamiento y logs.
