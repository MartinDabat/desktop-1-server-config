# Despliegue

Los despliegues coordinados se agregarán cuando haya más de un servicio con
dependencias de orden. Por ahora cada servicio se aplica mediante su propio
`deploy.sh` y requiere una acción explícita.
